作用：模拟飞行，以生成一段航空图像序列。
具体做法：将一张大的航空图假想为地面，以该图像的坐标系建立世界坐标系；设置一架模拟的飞机，在给定的出发点以给定的速度飞行；
		设置一个模拟的IMU，产生零均值的加速度和角速度；设定一个模拟的相机，由飞机的姿态计算相机的外参数，结合相机的内参数，
		计算单应矩阵，将地面图像对应的像素投影到相机平面，生成合成的图像序列。
		仿真中直接把重力假设为0，从而消除了重力的影响。
